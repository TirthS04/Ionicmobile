import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Notification_NewsApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
